{
  "project": "personal-library",
  "last_updated": "2026-05-28",
  "version": "0.1.0",
  "release_phase": "backend-catalog-partial",
  "domains": [
    {
      "name": "foundation",
      "status": "done",
      "quality_grade": "N/A",
      "features": [
        {
          "id": "repo-001",
          "name": "Repository state reconciliation",
          "status": "done",
          "tests": false,
          "notes": "Rewrote CLAUDE.md, STATUS.json, docs/architecture.md, docs/progress.md, docs/design.md, HANDOFF.md. Updated active skill files. Corrected broken architecture reference. No application code — docs/state only. Correction pass (2026-05-24): restored import scope as back-011, made status consistent across all trackers, corrected carryover docs."
        },
        {
          "id": "repo-002",
          "name": "Project scaffold and git init",
          "status": "done",
          "tests": false,
          "notes": "Created root .gitignore (Python, Node, env, logs, macOS artifacts). Scaffolded api/ (app/models, schemas, routers, services, alembic/versions, tests with all placeholder .py files) and web/ (src/app routes, components, lib). Git already initialized (root commit on main). No application logic — empty/minimal placeholders only."
        }
      ]
    },
    {
      "name": "backend-core",
      "status": "in_progress",
      "quality_grade": "N/A",
      "features": [
        {
          "id": "back-001",
          "name": "FastAPI setup — config, database, main",
          "status": "done",
          "tests": true,
          "notes": "Created pyproject.toml, .env.example, app/config.py, app/database.py, app/main.py, app/deps.py, and app/__init__.py. Added APIRouter stubs to all 6 router files and a foundation smoke test in tests/test_app.py. Created backend venv and installed dev dependencies. Final verified commands from api/: ruff pass, mypy pass, pytest pass. Test bootstrap no longer depends on an untracked local .env because tests seed required env vars in conftest.py before importing app.config."
        },
        {
          "id": "back-002",
          "name": "JWT auth — login endpoint and token verification",
          "status": "done",
          "tests": true,
          "notes": "app/auth.py: bcrypt hashing (hash_password, verify_password), JWT encode/decode (create_access_token, verify_token). app/routers/auth.py: POST /auth/login returns JWT on valid credentials, 401 on bad creds; GET /auth/me protected test endpoint. tests/test_auth.py: 3 tests (login success, wrong password → 401, protected endpoint without token → 401). Switched from passlib[bcrypt] to bcrypt directly (passlib incompatible with bcrypt>=5)."
        },
        {
          "id": "back-003",
          "name": "SQLAlchemy models and initial Alembic migration",
          "status": "done",
          "tests": false,
          "notes": "Models: Book (books, authors JSONB, Mapped[list[str]]), Tag + BookTag (tags, book_tags, ondelete=CASCADE on book_id, RESTRICT on tag_id), Loan (loans, book_id ondelete undefined — open decision for back-008), LabelTemplate (label_templates). SQLAlchemy 2 typed ORM with UUID PKs, timezone-aware datetimes. Alembic: alembic.ini, async env.py, concrete 0001_initial_schema.py (5 tables, FKs with ondelete rules, index on books.isbn_13). Added bcrypt to pyproject.toml. Corrective pass (2026-05-25): typed authors as list[str], aligned to JSONB, made FK ondelete policies explicit."
        },
        {
          "id": "back-004",
          "name": "pytest async fixtures with isolated test database",
          "status": "done",
          "tests": false,
          "notes": "tests/conftest.py: test_engine (session-scoped, drop/create schema), db_session, client (ASGITransport, DB override), auth_client (pre-authenticated). Test database created: personal_library_test on local PostgreSQL 18 (Homebrew)."
        }
      ]
    },
    {
      "name": "backend-books",
      "status": "in_progress",
      "quality_grade": "N/A",
      "features": [
        {
          "id": "back-005",
          "name": "ISBN EAN-13 validation and normalization",
          "status": "done",
          "tests": true,
          "notes": "services/isbn_validate.py: normalize_isbn() strips spaces/hyphens, returns digits-only for valid 10/13 digit shapes (full digit check), None otherwise. validate_isbn13() implements EAN-13 checksum. 7 unit tests pass (corrective pass 2026-05-25: fixed partial digit check bug)."
        },
        {
          "id": "back-006",
          "name": "ISBN lookup — Open Library with Google Books fallback",
          "status": "done",
          "tests": true,
          "notes": "services/isbn_lookup.py: BookData dataclass, lookup_isbn() (Open Library first, Google Books fallback), async httpx with 10s timeout. Google Books includes API key only if settings.google_books_api_key is set. Broad exception handling returns None. 3 respx-mocked integration tests pass."
        },
        {
          "id": "back-007",
          "name": "Books CRUD with ISBN lookup endpoint",
          "status": "done",
          "tests": true,
          "notes": "schemas/book.py: BookCreate, BookUpdate, BookOut, TagOut. routers/books.py: POST /books/ (201 + tag attach), GET /books/ (list with search/language/tag_id filters), GET /books/{id}, PATCH /books/{id} (partial update, tag replacement), DELETE /books/{id} (204), GET /books/lookup/{isbn} (validate + look up + fill isbn_13 from input). 12 tests pass (9 ORM, 3 HTTP with mocked lookup). Infrastructure: test_engine switched to function scope to fix asyncpg event-loop mismatch."
        }
      ]
    },
    {
      "name": "backend-catalog",
      "status": "in_progress",
      "quality_grade": "N/A",
      "features": [
        {
          "id": "back-008",
          "name": "Tags and loans CRUD",
          "status": "done",
          "tests": true,
          "notes": "schemas/tag.py: TagCreate, TagUpdate, TagOut. schemas/loan.py: LoanCreate, LoanReturn, LoanOut. routers/tags.py: POST/GET/PATCH/DELETE /tags/, graceful 409 on delete of in-use tag (passive_deletes=True on Tag.book_tags). routers/loans.py: POST /loans/ (validates book_id exists), GET /loans/ (ordered desc, open_only filter), POST /loans/{id}/return (defaults returned_at to UTC now). 12 HTTP tests pass. loans.book_id delete policy: resolved as implicit RESTRICT (preserves loan history, DB rejects book delete with active loans)."
        },
        {
          "id": "back-009",
          "name": "Label templates and PDF generation",
          "status": "done",
          "tests": true,
          "notes": "schemas/label_template.py: LabelTemplateCreate/Out, LabelGenerateRequest. services/pdf_labels.py: generate_labels_pdf with ReportLab Canvas + Code128 barcodes. routers/labels.py: POST/GET /labels/templates/, DELETE /labels/templates/{id}, POST /labels/generate (returns application/pdf). 8 HTTP tests pass (template CRUD, PDF generation, 404 edge cases)."
        },
        {
          "id": "back-010",
          "name": "BibTeX and CSV export",
          "status": "done",
          "tests": true,
          "notes": "services/bibtex_io.py: generate_bibtex(books) using bibtexparser (1.4 API: BibDatabase + dumps), cite keys from first author surname + year, deduplication. services/csv_io.py: generate_csv(books) using csv stdlib, 14-column output. routers/export.py: GET /export/csv (text/csv), GET /export/bibtex (application/x-bibtex), both with Content-Disposition header, auth required, ordered by title. tests/test_export.py: 6 HTTP tests — CSV with books, CSV empty, BibTeX with books, BibTeX empty, auth required for both endpoints."
        },
        {
          "id": "back-011",
          "name": "CSV and BibTeX import",
          "status": "pending",
          "tests": false,
          "notes": "MVP scope per approved spec (Module 6). CSV upload with column mapping (POST /import/csv). BibTeX upload to import existing catalog from Zotero/Mendeley (POST /import/bibtex). Part of routers/export.py or new routers/import_.py."
        }
      ]
    },
    {
      "name": "frontend-core",
      "status": "pending",
      "quality_grade": "N/A",
      "features": [
        {
          "id": "front-001",
          "name": "Next.js 15 scaffold — Tailwind v4, shadcn/ui, auth middleware",
          "status": "pending",
          "tests": false,
          "notes": "web/: create-next-app, shadcn init, @zxing/browser install. src/lib/api.ts, src/lib/auth.ts (server actions, httpOnly cookie). src/middleware.ts redirects unauthenticated users to /login."
        },
        {
          "id": "front-002",
          "name": "Login page",
          "status": "pending",
          "tests": false,
          "notes": "web/src/app/login/page.tsx. Client component. POST to /auth/login, stores token via server action, redirects to /catalog."
        }
      ]
    },
    {
      "name": "frontend-catalog",
      "status": "pending",
      "quality_grade": "N/A",
      "features": [
        {
          "id": "front-003",
          "name": "Book catalog page — grid/list view with search and filters",
          "status": "pending",
          "tests": false,
          "notes": "app/catalog/page.tsx. Search bar, tag/language/loan-status filters, sort controls. Grid (covers) and list toggle."
        },
        {
          "id": "front-004",
          "name": "Book detail page",
          "status": "pending",
          "tests": false,
          "notes": "app/catalog/[id]/page.tsx. All book fields, tag badges, loan history, edit and delete actions."
        },
        {
          "id": "front-005",
          "name": "Book registration with ISBN scanner",
          "status": "pending",
          "tests": false,
          "notes": "app/books/new/page.tsx + components/isbn-scanner.tsx (@zxing/browser). ISBN entry triggers /books/lookup/{isbn} pre-fill. Manual entry fallback."
        },
        {
          "id": "front-006",
          "name": "Loans page",
          "status": "pending",
          "tests": false,
          "notes": "app/loans/page.tsx. Open loans list with days elapsed, return action, loan history per book."
        },
        {
          "id": "front-007",
          "name": "Label generation page",
          "status": "pending",
          "tests": false,
          "notes": "app/labels/page.tsx. Select books, choose template, POST /labels/generate, trigger PDF download."
        }
      ]
    }
  ],
  "architectural_drift": [],
  "open_decisions": [
    "Confirm PostgreSQL install method for local dev: Docker or native.",
    "Confirm Google Books API key requirement (optional in spec — key is optional, lookup still works without it).",
    "Production deployment details: nginx config, systemd units, env management on VPS."
  ]
}
